<Page
    x:Class="LottieUwpPlayground.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:lottie="using:Lottie"
    mc:Ignorable="d">

    <Grid Background="Gray" AllowDrop="True" DragOver="LottieDragOverHandler" Drop="LottieDropHandler">

        <Grid x:Name="Host" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
            <lottie:LottiePlayer x:Name="lottiePlayer" 
                    IsLoopingEnabled="True" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                    BackgroundColor="{x:Bind backgroundColorPicker.Color, Mode=OneWay}"   
                    AutoPlay="True"
                             />
            <StackPanel VerticalAlignment="Bottom">
                <StackPanel.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="HorizontalTextAlignment" Value="Center"/>
                    </Style>
                </StackPanel.Resources>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="{x:Bind lottiePlayer.FileName, Mode=OneWay}" TextAlignment="Right"/>
                    <TextBlock Margin="10,0,0,0" Text="{x:Bind lottiePlayer.Duration, Mode=OneWay}" TextAlignment="Left"/>
                </StackPanel>
                <TextBlock Text="{x:Bind lottiePlayer.State, Mode=OneWay}" />

                <TextBlock Text="{x:Bind lottiePlayer.Diagnostics, Mode=OneWay}" TextWrapping="Wrap" Height="Auto"/>
            </StackPanel>
        </Grid>

        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Center" >
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Margin" Value="10"/>
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                    <Setter Property="BorderBrush" Value="Black"/>
                    <Setter Property="Background" Value="LightGray"/>
                </Style>
                <Style TargetType="ToggleSwitch">
                    <Setter Property="Margin" Value="10"/>
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                    <Setter Property="BorderBrush" Value="Black"/>
                    <Setter Property="Background" Value="LightGray"/>
                </Style>
                <Style TargetType="Slider">
                    <Setter Property="Margin" Value="10"/>
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                    <Setter Property="BorderBrush" Value="Black"/>
                    <Setter Property="Background" Value="LightGray"/>
                </Style>
            </StackPanel.Resources>


            <!--<Button Click="PickFile_Click">Pick Lottie file</Button>-->

            <TextBlock TextAlignment="Center">Drag lottie file onto Window to play</TextBlock>
            <!--<Button x:Name="PlayButton" Click="PlayButton_Click">Play</Button>-->

            <!--<Button x:Name="PauseButton" Click="PauseButton_Click">Pause</Button>-->

            <ToggleSwitch x:Name="LoopingSwitch"      
                    OnContent="Looping enabled" OffContent="Looping disabled"
                    IsOn="{x:Bind lottiePlayer.IsLoopingEnabled, Mode=TwoWay}"/>

            <!--<Slider x:Name="SpeedSlider"
                    Minimum="-2"
                    Maximum="2"
                    TickFrequency="0.5"/>-->

            <!--<Slider x:Name="ProgressSlider" StepFrequency="0.01"/>-->

            <Grid>
                <ColorPicker x:Name="backgroundColorPicker"
                     ColorSpectrumShape="Box"
                     IsColorPreviewVisible="False"
                     IsColorChannelTextInputVisible="False"
                     IsHexInputVisible="False" />
                <TextBlock IsHitTestVisible="False" >Background color</TextBlock>
            </Grid>
        </StackPanel>

    </Grid>
</Page>
