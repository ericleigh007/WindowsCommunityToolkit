<UserControl
    x:Class="LottieViewer.Stage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LottieViewer"
    xmlns:lottie="using:Lottie"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    >
    <Grid>
        <local:FeedbackLottie x:Name="_feedbackLottie"/>
        <Grid Margin="100" >
            <lottie:CompositionPlayer x:Name="_player" LoopAnimation="True"  AutoPlay="False" BackgroundColor="{x:Bind Path=ArtboardColor, Mode=OneWay}" >
                <lottie:LottieCompositionSource x:Name="_playerSource" Options="All"/>
            </lottie:CompositionPlayer>
        </Grid>
        <StackPanel VerticalAlignment="Bottom" Margin="24">
            <TextBlock x:Name="_txtFileName"/>
            <TextBlock x:Name="_txtDuration"/>
            <TextBlock x:Name="_txtSize"/>
        </StackPanel>
    </Grid>
</UserControl>
